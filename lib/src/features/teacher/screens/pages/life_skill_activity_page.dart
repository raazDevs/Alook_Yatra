import 'package:flutter/material.dart';

class LifeSkillActivitiesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Life Skill Activities')),
      body: Center(child: Text('Life Skill Activities Page')),
    );
  }
}
